package package0;

import java.util.ArrayList;
import java.util.Collections;

public class GeneticProgram {
	public static ArrayList<Long> compute(ArrayList<Long> values00) {
		{
			ArrayList<Long> values01 = new ArrayList<Long>(Collections.nCopies(values00.size(), new Long(0)));
			Long value00;
			ArrayList<Boolean> conditions00 = new ArrayList<Boolean>(Collections.nCopies(values00.size(),false));
			Boolean condition00;
	
			values01.set( (0+1)*((-1)*(1-2)) , new Long( values00.size() ));
			value00 = new Long(0);
			conditions00.set( 0 , new Boolean(false));
			condition00 = false;
			
			if((!condition00)) { } else { }
			while((condition00)) { }
			
		}
		return values00;
	} 
}
